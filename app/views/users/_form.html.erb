<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_picture, %>
    <%= form.text_field :user_picture,, id: :user_user_picture, %>
  </div>

  <div class="field">
    <%= form.label :name, %>
    <%= form.text_field :name,, id: :user_name, %>
  </div>

  <div class="field">
    <%= form.label :password, %>
    <%= form.text_field :password,, id: :user_password, %>
  </div>

  <div class="field">
    <%= form.label :top_5_restaurants %>
    <%= form.text_field :top_5_restaurants, id: :user_top_5_restaurants %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
